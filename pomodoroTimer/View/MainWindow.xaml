<Window x:Class="pomodoroTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:pomodoroTimer.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="550" Background="#ed2939">

    <Window.DataContext>
        <viewModel:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="StyleTemplate.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <!--<Behaviors:Interaction.Triggers>
            <Behaviors:EventTrigger EventName="MouseDoubleClick" >
                <Behaviors:CallMethodAction TargetObject="{Binding}" MethodName="ListBox_MouseDoubleClick"/>
            </Behaviors:EventTrigger>
        </Behaviors:Interaction.Triggers>-->

        <DockPanel>
            <TextBlock Text="Pomodoro App" DockPanel.Dock="Top" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold" Foreground="white" Margin="0 25 0 0" />
            <DockPanel DockPanel.Dock="Top" Background="#fa8072" Width="450" Height="400" Opacity="0.9" >
                <DockPanel Background="#ed2939" Width="120" Height="auto" DockPanel.Dock="Top" Margin="0 20 0 10"  >
                    <TextBlock Text="Session"  Foreground="white" HorizontalAlignment="center" FontSize="30" />
                </DockPanel>
                <DockPanel DockPanel.Dock="Top" HorizontalAlignment="center">
                    <TextBlock Text="{Binding Path=TextBlock}" FontSize="60" FontWeight="Bold" Foreground="white" />
                </DockPanel>
                <DockPanel Width="230" Height="100">
                    <Button Command="{Binding Path=StartTimerCommand}" Content="{Binding Path=StartButton}" FontSize="50" Foreground="white" Background="#fa8072" Width="100" Height="80" Cursor="Hand" />
                    <Button Command="{Binding Path=ResetTimerCommand}" Content="🔄" FontSize="50" Foreground="white" Background="#fa8072" BorderBrush="#fa8072" Width="100" Height="80" Cursor="Hand" HorizontalAlignment="Right" />
                </DockPanel>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
